#include <stdio.h>

void main() {
   int num = 0;
   int par, result;
   
   
   for (result == num>=20; num + (par= num % 2 == 0); num++);
   printf("a soma de %d + %d = %d \n", num, par, result);
   
 
}
 